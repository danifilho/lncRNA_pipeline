Bootstrap: docker
From: mambaorg/micromamba:1.5.10

%post
    micromamba install -y -n base -c conda-forge -c bioconda fastp=0.23.4
    micromamba clean -a -y

%environment
    export PATH=/opt/conda/bin:$PATH
    export LC_ALL=C

%runscript
    exec fastp "$@"

